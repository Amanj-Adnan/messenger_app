<div class="text-indigo-700 text-m font-sans font-bold">
<%= form_with(model: room ,class:"w-full max-w-xs bg-white flex flex-col py-5 px-8 rounded-lg shadow-lg") do |form| %>
  <% if room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
        <% room.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div class="">
    <%= form.label :name %>
    <%= form.text_field :name ,class:"text-gray-700 shadow border rounded border-gray-300 focus:outline-none focus:shadow-outline py-1 px-3 mb-3"%>
  </div>

  <div class="">
    <%= form.submit "Add Room" , class:"bg-indigo-500 hover:bg-indigo-700 text-white font-bold rounded py-2 px-4"%>
  </div>
<% end %>
</div>